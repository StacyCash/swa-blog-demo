{
  "routes": [
    {
      "route": "/api/blogposts",
      "method": ["POST", "PUT"],
      "allowedRoles": ["admin"]
    },
    {
      "route": "/api/blogposts",
      "method": ["GET"]
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": ["/images/*.{png,jpg,gif}", "/css/*", "/api/*"]
  }
}
