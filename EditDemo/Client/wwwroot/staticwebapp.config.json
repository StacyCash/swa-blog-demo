{
  "routes": [
    {
      "route": "/api/blogposts",
      "method": ["POST", "PUT"],
      "allowedRoles": ["admin"]
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": ["/images/*.{png,jpg,gif}", "/css/*", "/api/*"]
  }
}
