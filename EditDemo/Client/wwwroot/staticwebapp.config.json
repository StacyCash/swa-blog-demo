{
  "routes": [
    {
      "route": "/api/blogposts",
      "methods": ["POST", "PUT"],
      "allowedRoles": ["admin"]
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": ["/images/*.{png,jpg,gif}", "/css/*", "/api/*"]
  }
}
