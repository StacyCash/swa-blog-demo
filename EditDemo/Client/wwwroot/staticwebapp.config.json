{
  "routes": [
    {
      "route": "/api/blogposts",
      "methods": ["POST", "PUT"],
      "allowedRoles": ["admin"]
    },
    {
      "route": "/api/blogposts",
      "methods": ["GET"]
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": ["/images/*.{png,jpg,gif}", "/css/*", "/api/*"]
  }
}
